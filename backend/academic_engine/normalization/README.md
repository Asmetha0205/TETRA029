# Academic Technology Normalization

Academic wrapper delegating to the Industry Intelligence Engine's `NormalizationPipeline` and `TechnologyRegistry`. Normalizes aliases, canonical names, and categories without duplicating logic.
