# Recommendation Engine Configuration

Centralized configuration for Phase 6 Recommendation Intelligence Layer.

## Settings
- `Neo4jConfig`: Graph database connection credentials, URI, pool configuration.
- `LLMRecommendationConfig`: Gemini API key, model selection, temperature, retries.
- `RecommendationEngineConfig`: Service settings, memory fallbacks, caching parameters.
